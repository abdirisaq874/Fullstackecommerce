/**
 * THIS IS A PLACEHOLDER. Generated types live here.
 * Run `npm run codegen:api` with the backend running to populate this file.
 * Until then, the file exports an empty schema so the codebase compiles.
 */
export interface paths {}
export interface components { schemas: Record<string, unknown> }
export type operations = Record<string, never>;
